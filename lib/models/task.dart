class Task {
  String id;
  String title;
  String description;
  DateTime time;
  bool isCompleted;

  Task({
    this.id,
    this.title,
    this.description,
    this.isCompleted,
    this.time,
  });
}
